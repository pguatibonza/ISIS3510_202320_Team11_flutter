import 'package:flutter/material.dart';

class CurrentTripScreen extends StatelessWidget {
  const CurrentTripScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child:Text("Current Trips Screen"));
  }
}